# Changelog

本文件只放「当次要发布的版本」的说明（不含历史）。发布前把新版本条目写在这里，供 `release-gh.sh` 作为 Release 说明使用；完整历史可在此文件追加。

## v1.0.0
- 首个公开版本。
- 基于 yt-dlp 的媒体下载（视频/音频，多画质与格式，并发队列）。
- YouTube RSS 订阅与更新通知（系统通知 + 收件箱）。
- 应用内播放器与外部播放器兜底（WebM 等格式自动走外部播放器）。
- 订阅批量操作与备份导出/导入。
