Soluna 安装说明
====================

1. 把 Soluna.app 拖到「应用程序」(/Applications) 文件夹

2. 打开「终端」（Spotlight 搜"终端"），执行以下命令并输入登录密码：
   sudo xattr -r -d com.apple.quarantine /Applications/Soluna.app

3. 在「应用程序」文件夹里找到 Soluna.app，第一次打开时
   在弹窗中点"打开"即可（只需这一次，之后双击正常启动）
